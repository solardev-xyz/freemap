/**
 * FreeMap — Swarm tile manifest reference.
 * Tile PNGs live on Swarm at bzz://<tileRoot>/ — this repo only ships the viewer shell.
 */
window.FREEMAP_CONFIG = {
	tileRoot: 'ab77201f6541a9ceafb98a46c643273cfa397a87798273dd17feb2aa366ce2e6',
	defaultBeeGateway: 'http://127.0.0.1:1633',
	defaultPublicGateway: 'https://api.gateway.ethswarm.org',
	defaultLat: 51.4934,
	defaultLon: 0.0098,
}
